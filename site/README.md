# Karaoke dashboard

`python scripts/karaoke/build_dashboard.py` reads the normalized performance records and regenerates `site/data.json`.

The dashboard intentionally shows only the 20 most-played songs overall and the 10 most-played songs within each year.
