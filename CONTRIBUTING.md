# Contributing

자료를 추가하거나 수정할 때는 다음 원칙을 지킵니다.

1. Setlist Index 원본은 로컬에서만 보관합니다.
2. 로컬 importer로 후보·정규화 JSON과 source metadata를 생성합니다.
3. 원본 출처 URL과 metadata를 기록합니다.
4. 방송, 공연 곡, 원곡을 별도 항목으로 관리합니다.
5. 불확실한 항목은 needs-review로 남깁니다.
6. 자동 생성 파일을 직접 수정하지 않습니다.
7. 변경 전후 diff와 validation 결과를 확인합니다.
8. 생성된 JSON·metadata·playlist manifest만 GitHub에 업로드합니다.

AI agent의 변경은 가능한 한 Pull Request로 검토합니다.
