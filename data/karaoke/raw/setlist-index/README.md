# Setlist Index raw snapshots

Store source-shaped snapshots retrieved from Setlist Index here.

Each snapshot must include or be accompanied by:

- retrieval date
- source URL
- source identifier or video ID
- importer/parser version
- retrieval result or error

Do not overwrite an earlier snapshot. Normalized records belong under data/karaoke/normalized/.
