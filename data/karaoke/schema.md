# Karaoke Data Schema

`broadcasts.json` stores broadcasts. `songs.json` stores performances within broadcasts. They remain separate so one broadcast can contain multiple performances and one original song can be performed in multiple broadcasts.

Every published record must include an ID, source evidence, and one of the statuses defined in `AGENTS.md`.
